<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp">

    <TextView
        android:id="@+id/tvResSin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/sin_viewmodel_0"
        android:textSize="24sp"
        android:gravity="center"
        android:layout_marginBottom="24dp"
        tools:ignore="HarcodedText"/>

    <TextView
        android:id="@+id/tvResCon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/con_viewmodel_0"
        android:textSize="24sp"
        android:gravity="center"
        android:layout_marginBottom="24dp"
        tools:ignore="HarcodedText"/>
    <Button
        android:id="@+id/btnIncrementarSin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Incrementar"
        tools:ignore="HardcodedText"
        android:layout_marginBottom="16dp"/>

    <Button
        android:id="@+id/btnIncrementarCon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Incrementar estable"
        tools:ignore="HardcodedText"
        android:layout_marginBottom="16dp"/>

</LinearLayout>